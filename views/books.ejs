<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Book List</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css" />
  <style>
    :root {
      --primary: #4f46e5;
      --primary-hover: #4338ca;
      --danger: #ef4444;
      --danger-hover: #dc2626;
      --gray-100: #f3f4f6;
      --gray-200: #e5e7eb;
      --gray-600: #4b5563;
      --gray-800: #1f2937;
      --success: #10b981;
      --border-radius: 0.5rem;
      --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
      --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
      --transition: all 0.2s ease-in-out;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background-color: #f9fafb;
      color: var(--gray-800);
      line-height: 1.6;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 2rem 1rem;
    }

    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
      flex-wrap: wrap;
      gap: 1rem;
    }

    h1 {
      font-size: 1.875rem;
      font-weight: 700;
      color: var(--gray-800);
    }

    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0.5rem 1rem;
      font-size: 0.875rem;
      font-weight: 500;
      border-radius: var(--border-radius);
      text-decoration: none;
      cursor: pointer;
      transition: var(--transition);
      border: none;
      gap: 0.5rem;
    }

    .btn-primary {
      background-color: var(--primary);
      color: white;
    }

    .btn-primary:hover {
      background-color: var(--primary-hover);
      transform: translateY(-1px);
    }

    .btn-danger {
      background-color: var(--danger);
      color: white;
      font-size: 0.75rem;
      padding: 0.25rem 0.75rem;
    }

    .btn-danger:hover {
      background-color: var(--danger-hover);
    }

    .btn-link {
      color: var(--primary);
      font-weight: 500;
    }

    .btn-link:hover {
      text-decoration: underline;
    }

    .search-section {
      background: white;
      padding: 1.5rem;
      border-radius: var(--border-radius);
      box-shadow: var(--shadow-sm);
      margin-bottom: 2rem;
    }

    .search-section h2 {
      font-size: 1.25rem;
      margin-bottom: 1rem;
      color: var(--gray-800);
    }

    .form-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 1rem;
      margin-bottom: 1rem;
    }

    .form-group {
      display: flex;
      flex-direction: column;
    }

    .form-group label {
      margin-bottom: 0.5rem;
      font-weight: 500;
      color: var(--gray-600);
      font-size: 0.875rem;
    }

    .form-group input {
      padding: 0.625rem;
      border: 1px solid var(--gray-200);
      border-radius: var(--border-radius);
      font-size: 1rem;
      transition: var(--transition);
    }

    .form-group input:focus {
      outline: none;
      border-color: var(--primary);
      box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.1);
    }

    .search-actions {
      display: flex;
      gap: 0.75rem;
      margin-top: 0.5rem;
    }

    .book-list {
      list-style: none;
    }

    .book-item {
      background: white;
      padding: 1.25rem;
      border-radius: var(--border-radius);
      box-shadow: var(--shadow-sm);
      margin-bottom: 1rem;
      transition: var(--transition);
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .book-item:hover {
      box-shadow: var(--shadow);
      transform: translateY(-2px);
    }

    .book-info h3 {
      font-size: 1.125rem;
      font-weight: 600;
      color: var(--gray-800);
      margin-bottom: 0.25rem;
    }

    .book-meta {
      font-size: 0.875rem;
      color: var(--gray-600);
    }

    .book-meta span {
      display: inline-block;
      margin-right: 1rem;
    }

    .book-actions {
      display: flex;
      gap: 0.5rem;
      align-items: center;
    }

    .genre-tag {
      display: inline-block;
      background-color: var(--gray-100);
      color: var(--gray-700);
      padding: 0.25rem 0.5rem;
      border-radius: 0.375rem;
      font-size: 0.75rem;
      font-weight: 500;
    }

    @media (max-width: 640px) {
      .book-item {
        flex-direction: column;
        align-items: flex-start;
      }

      .book-actions {
        width: 100%;
        justify-content: flex-end;
      }

      .form-grid {
        grid-template-columns: 1fr;
      }
    }

    .no-books {
      text-align: center;
      padding: 3rem 1rem;
      color: var(--gray-600);
      font-style: italic;
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <h1>📚 Book List</h1>
      <div>
        <% if (role === 'admin') { %>
          <a href="/books/new" class="btn btn-primary">+ Add New Book</a>
        <% } %>
        <a href="/logout" class="btn btn-link" style="margin-left: 1rem;">Logout</a>
      </div>
    </header>

    <section class="search-section">
      <h2>Search Books</h2>
      <form action="/books" method="get">
        <div class="form-grid">
          <div class="form-group">
            <label for="title">Title</label>
            <input type="text" id="title" name="title" value="<%= searchQuery?.title || '' %>" placeholder="Enter book title">
          </div>
          <div class="form-group">
            <label for="author">Author</label>
            <input type="text" id="author" name="author" value="<%= searchQuery?.author || '' %>" placeholder="Enter author name">
          </div>
          <div class="form-group">
            <label for="year">Year</label>
            <input type="number" id="year" name="year" value="<%= searchQuery?.year || '' %>" placeholder="e.g., 2023" min="1500" max="2100">
          </div>
          <div class="form-group">
            <label for="genre">Genre</label>
            <input type="text" id="genre" name="genre" value="<%= searchQuery?.genre || '' %>" placeholder="e.g., Fiction, Sci-Fi">
          </div>
        </div>
        <div class="search-actions">
          <button type="submit" class="btn btn-primary">🔍 Search</button>
          <a href="/books" class="btn btn-link">Clear Filters</a>
        </div>
      </form>
    </section>

    <% if (books.length > 0) { %>
      <ul class="book-list">
        <% books.forEach(book => { %>
          <li class="book-item">
            <div class="book-info">
              <h3><%= book.title %></h3>
              <div class="book-meta">
                <span>✍️ <%= book.author %></span>
                <span>📅 <%= book.year %></span>
                <span class="genre-tag"><%= book.genre %></span>
              </div>
            </div>
            <% if (role === 'admin') { %>
              <div class="book-actions">
                <a href="/books/<%= book._id %>/edit" class="btn btn-link">Edit</a>
                <form action="/books/<%= book._id %>/delete" method="post" style="display: inline;">
                  <button type="submit" class="btn btn-danger" 
                          onclick="return confirm('Are you sure you want to delete &quot;<%= book.title %>&quot;?');">
                    Delete
                  </button>
                </form>
              </div>
            <% } %>
          </li>
        <% }); %>
      </ul>
    <% } else { %>
      <p class="no-books">No books found matching your search. Try adjusting the filters.</p>
    <% } %>
  </div>
</body>
</html>
